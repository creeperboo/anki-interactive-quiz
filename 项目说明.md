# 互动答题卡 · 项目说明（接续用）

> 这份文档是给「下次接手这个项目的 AI 或你自己」看的。
> 目标：几分钟内搞清项目是什么、文件在哪、怎么改、怎么验证、还有什么没定。

最后更新：2026-09-24
当前版本：1.1.7（检查更新能分辨「装好了但还没重启」：磁盘版本比运行中的新就提示重启，不再催下载；
1.1.6 的内容见下：同标签技巧候选不显示题目、按文字去重；知识点改成「一行一条 标签 -> 链接」，
答完题点标签跳转，链接在编辑器里逐行配）

---

## 1. 一句话说明

一个 **Anki 桌面版插件**，让选择题 / 判断题 / 填空题可以直接在复习界面作答：

- 点「不知道」→ 强制记 **重来**
- 答错 → 强制记 **重来**
- 答对 → 自己选 **困难 / 良好 / 简单**

另带一个 **工具 → 互动答题卡：设置与统计…** 窗口（设置 4 项 + 卡片级统计表）。

---

## 2. 当前状态

### 已完成

| 功能 | 说明 |
| --- | --- |
| 题型 | 启动时自动创建/更新 3 个独立题型：**互动答题卡·选择题**、**互动答题卡·判断题**、**互动答题卡·填空**（原生 cloze） |
| 选择题 | 字段：题目/选项/解析/解题技巧/来源/知识点；行首 `*` 标正确项；1 个 `*` = 单选（点一下即判分），多个 = 多选（点提交，全对才算对） |
| 判断题 | 字段：题目/解析/解题技巧/来源/知识点；对/错存在题目末尾的隐藏标记里，卡片上出现「正确 / 错误」两个按钮 |
| 原生填空题 | 第二个题型「互动答题卡·填空」，`type = 1`（cloze）：题目里写 `{{c1::答案}}`，一个 c 号一张卡，编辑器里能用挖空按钮 / Ctrl+Shift+C |
| 解题技巧 | 字段「解题技巧」，答完跟着「解析」一起显示；答错一定显示，答对时受 `reveal_explanation_on_correct` 控制；背面永远显示 |
| 一键用同标签卡片的技巧（1.1.6 起） | 编辑器里「解题技巧」下面有 [⚡ 用同标签卡片的技巧]：拿当前卡片的标签去找同标签、且写了技巧的其他卡片，**一律弹候选列表**（按共同标签数、新近排序），点 [用这条] 填入。候选**只显示技巧正文 + 共同标签，不再显示题目**；**文字一样的技巧只留一条**（空格、换行、全角空格、HTML 标签都不算），留的是排最前面的那张卡。标签框里还没提交的标签也会被带上 |
| 相关知识点（1.1.6 起改成点标签跳转） | 字段「知识点」**一行一条 `标签 -> 链接`**（`→` 也认，只写链接的老写法照样有效），每行都生效、标签重复只留第一条。答完题后反馈区与答案面显示 `📚 相关知识点` + 一排**可点的标签**，点谁跳谁（网址 → 系统浏览器；其它 → 卡片浏览器搜索；手机上网页链接是普通 `<a>`）。编辑器里「知识点」下面有 [🏷 配置标签链接]（默认收起）：点开就是这张卡的标签一行一个输入框 + [试打开] + [×]，另有 [🔄 刷新标签]，字段里的老内容一律保留 |
| 编辑器里的选项编辑器 | 选择题：把「选项」字段块藏起来，换成一行一个输入框 + 右侧「正确」勾选框；[＋ 添加选项] / Enter 加行、空行 Backspace 删行、Alt+↑↓ 排序、Alt+数字 勾选、粘贴自动拆行；实时状态「单选 / 多选 / 还没勾选正确答案」 |
| 编辑器里的判断题勾选框 | 判断题：把「答案」整个字段块藏起来，换成一个「这题是正确的」勾选框（勾=对、不勾=错），旁边显示当前值 |
| 字段瘦身（1.1.3 / 1.1.4） | **三个题型都没有「答案」字段**：选择题正误看选项行首的 `*`；填空题答案在 `{{c1::}}` 里；判断题的对/错写成 `题目` 末尾的隐藏标记 `<!--iq-tf:对-->`（1.1.2 曾把「答案」补回来，1.1.3 删并迁移真值，1.1.4 才把判断题/填空题残留的空字段真正删掉） |
| 填空题状态行 | 显示「共 N 个空 → N 张卡」，没写 `{{c1::}}` 会提示 |
| 旧题型清理 | 通用题型「互动答题卡」已废弃：启动时若它里面**只剩示例卡片（或为空）就整型删除**，有真实笔记则保留并打日志；「类型」字段同样只在不含内容时才删 |
| 答完的答案呈现（1.1.5） | **不再单独列「正确答案：…」**：选择题 / 判断题靠选项着色（绿 = 该选、红 = 选错）；填空题把标准答案直接写进原来的空里，答错时先划掉你填的（`南京 → 北京`），没填就只写标准答案。反馈区与答案面同一套规则 |
| 答案面 | 题目 + 解析 + 解题技巧 + 来源；选择题/判断题画出着色选项，填空题答案在空里，不需要插件也能看 |
| 评级规则 | 答错/不知道 → 强制 Again；漏选 → 最多 Hard（有开关）；用 `min(用户选择, 允许上限)` 实现 |
| 设置窗口 | 4 项：记录明细 / 保留天数 / 显示计时 / 漏选记困难 + 维护按钮 |
| 统计窗口 | 卡片级表格，牌组（含子牌组）/ 题型 / 标签 / 时间 / 关键词 组合筛选，默认按难度排序 |
| 统计存储 | 插件目录下 `user_files/stats.db`（SQLite），按 Anki 用户配置隔离 |

### 未做 / 待定

见第 9 节。简单说：**手机端适配** 和 **统计跨设备同步** 都还没做，用户在两者上都还没拍板。

---

## 3. 文件结构

```
互动答题卡/                        ← 本文件夹（桌面）
├── 项目说明.md                    ← 你正在看的这份
├── 对话记录.md                    ← 这次对话的来龙去脉与决策
├── 互动答题卡-操作指南.txt         ← 给用户看的纯文本说明（UTF-8 BOM + CRLF）
├── README.md / README.en.md       ← 给 GitHub 看的中英文简介
├── version.txt                    ← 当前版本号（插件自更新检查用）
├── 安装到Anki.py                  ← 一键安装到 Anki 并重新打包
├── 一键更新插件到Anki-1.1.7.cmd    ← 双击版：调用 安装到Anki.py（路径用 %~dp0，只覆盖插件 8 个文件）
├── interactive_quiz.ankiaddon     ← 分发包（zip，换电脑时双击安装）
├── 答题反馈预览-1.1.7.html         ← 浏览器里看答完效果的预览页（自包含：css/js 以 base64 内嵌）
├── 互动答题卡-源码更新-1.1.7.zip   ← 整个项目文件夹的打包（发给别的机器 / 留档）
│                                     （上一版的 1.1.5 预览页 / zip 还留在文件夹里，可随手删）
├── run-js-tests.js                ← 卡片端 / 编辑器测试：node run-js-tests.js [源码目录] [quiz|editor]
├── check-preview.js               ← 预览页自动核对：跑真实 quiz.js 打 7 张卡的状态
├── build-preview.py               ← 重新生成预览页
├── build-zip.py                   ← 重新打包「源码更新」zip
├── normalize-eol.py               ← 把文本文件行尾统一成 CRLF（保留原来的 BOM 有无）
└── 源码/
    ├── manifest.json              ← 插件清单
    ├── config.json                ← 默认配置（13 项）
    ├── config.md                  ← Anki 自带配置界面的说明文字
    ├── README.md                  ← 插件自带说明（Markdown 版）
    ├── __init__.py                ← 全部 Python 逻辑（约 1000 行，刻意单文件）
    ├── assets/
    │   ├── quiz.js                ← 卡片端脚本（判分、交互、上报）
    │   ├── quiz.css               ← 卡片端样式
    │   └── editor.js              ← 编辑器助手（选项行控件 + 判断题勾选框 + 同标签技巧 + 知识点预览）
    └── tests/
        ├── py-test.py             ← Python 侧测试（270 项）
        ├── dom-test.js            ← 卡片端交互测试（208 项）
        └── editor-test.js         ← 编辑器助手测试（123 项）
```

**这些工具都"跟着项目走"**：`run-js-tests.js` / `check-preview.js` / `build-preview.py` / `build-zip.py` 都用「脚本自己所在目录」定位源码和输出，所以整个文件夹搬到哪都能直接跑，不带参数也行：

```powershell
node run-js-tests.js            # 卡片端 208 项
node run-js-tests.js 源码 editor # 编辑器助手 123 项
node check-preview.js           # 预览页里 7 张卡的状态
python build-preview.py         # 重新生成 答题反馈预览-1.1.7.html（内嵌当时的 quiz.js/css）
python build-zip.py             # 重新打包 互动答题卡-源码更新-1.1.7.zip
```

**为什么 Python 只有一个文件**：Anki 加载插件时是把它当包导入的，相对导入虽然能用，但为了彻底避开导入机制的风险，全部逻辑写在 `__init__.py` 里；JS/CSS 用 `Path(__file__).parent / "assets"` 在运行时读取并内联进卡片模板。

> 注意：卡片模板里**内联**了完整的 quiz.js（约 36 KB），所以每次改 JS 都必须重新执行一次「创建 / 更新题型模板」，否则卡片里跑的还是旧代码。插件启动时会自动做这件事。

---

## 4. 安装 / 更新流程

### 已安装位置

```
C:\Users\<你的用户名>\AppData\Roaming\Anki2\addons21\interactive_quiz\
```

### 用脚本安装（推荐）

```
"C:\Program Files\Lenovo\ModelMgr\Plugins\Image\python.exe" "安装到Anki.py"
```

它会：把 `源码/` 复制到 addons21 → 重建 `interactive_quiz.ankiaddon` → 打印校验信息。

不想敲命令就直接**双击 `一键更新插件到Anki-1.1.7.cmd`**（它内部就调用上面这条命令；用 `%~dp0`
找同目录的 `安装到Anki.py`，所以整个文件夹搬走也照样能用；只覆盖插件那 8 个文件，不动 `user_files`）。

### 手动安装

直接把 `源码/` 里的内容复制进 `addons21\interactive_quiz\`，或者双击 `.ankiaddon`。

### 生效

插件只在 Anki 启动时加载，**改完必须重启 Anki**。重启后插件会自动更新题型模板。

---

## 5. 技术架构

### 5.1 消息协议（卡片端 → 插件，`pycmd` 发送）

| 消息 | 含义 |
| --- | --- |
| `iq:reset` | 这张卡开始显示，清掉该卡的临时状态 |
| `iq:result:<mode>:<correct>:<dontknow>:<ms>:<partial>` | 本题判定结果，用于统计。mode = `single`/`multi`/`tf`/`fill` |
| `iq:wrong` | 答错了：这张卡最多只能记「重来」 |
| `iq:partial` | 漏选：这张卡最多只能记「困难」 |
| `iq:grade:<1-4>` | 用户选了某个评级（1 重来 / 2 困难 / 3 良好 / 4 简单） |
| `iq:config` | 取配置（带回调，目前未使用，改用注入方式） |
| `iq:open:<encodeURIComponent(目标)>` | 点了卡片上的「相关知识点」标签：网址开系统浏览器，其它当 Anki 搜索式 |

编辑器页面（`assets/editor.js`）发回来的：

| 消息 | 含义 |
| --- | --- |
| `iq:editor:set:<字段序号>:<encodeURIComponent(内容)>` | 控件改了字段（选项行 / 判断题答案） |
| `iq:editor:refresh` | 用户在原生字段里打了字，把最新字段值推回页面（填空状态行、知识点预览） |
| `iq:editor:tips:<encodeURIComponent(标签数组 JSON)>` | 点了「用同标签卡片的技巧」（带上标签框里还没提交的标签） |
| `iq:editor:open:<encodeURIComponent(目标)>` | 点了知识点面板里某一行的「试打开」 |
| `iq:editor:tf:<对\|错>` | 判断题勾选框：把真值写成题目末尾的隐藏标记（1.1.3） |

### 5.2 Python 侧状态（模块级）

| 变量 | 作用 |
| --- | --- |
| `_forced_ease: dict[card_id, int]` | 这张卡允许的最高评级 |
| `_pending_result: dict[card_id, dict]` | 已判定、等真正评级时写进统计的结果 |
| `_recent_grade: dict[card_id, float]` | 0.4 秒防重复点击 |

### 5.3 用到的 Anki 钩子

| 钩子 | 用途 |
| --- | --- |
| `webview_did_receive_js_message` | 接收所有 `iq:*` 消息（过滤器，返回 `(True, None)` 表示已处理） |
| `card_will_show` | 把配置 JSON 注入卡片模板（替换 `/*ANKI_QUIZ_CONFIG_MARKER*/`） |
| `reviewer_will_answer_card` | 把评级压到允许上限（**这是「答错绕不过去」的关键**） |
| `reviewer_did_answer_card` | 清状态 + 写一条统计 |
| `reviewer_did_show_question` | 清状态（兜底） |
| `profile_did_open` | 装菜单 + 建题型 + 清理过期统计 |
| `main_window_did_init` | 装菜单（兜底） |

### 5.4 依赖的 Anki 内部接口（已在 Anki 26.9.2 上核实存在）

| 接口 | 说明 |
| --- | --- |
| `Reviewer._answerCard(ease)` | 评级入口。取不到时回退到 `bottom.bottomWeb.eval("pycmd('easeN')")` |
| `pycmd` / `bridgeCommand` | 由 `AnkiWebPage._setupBridge` 注入到每个 webview 页面，卡片模板里可用 |
| `reviewer_will_answer_card` | 新签名是 `(ease_tuple, reviewer, card) -> (bool, ease)`；旧版是 `(ease, reviewer, card) -> ease`，代码里两套都兼容 |
| `mw.col.models.*` | `new_field/add_field/new_template/add_template/add/save` |
| `col.decks.all_names_and_ids()` / `.children(did)` / `.name(did)` / `.id(name)` | 牌组筛选 |
| `col.db.list(...)` / `col.tags.all()` | 标签筛选 |
| `mw.addonManager.getConfig / writeConfig` | 读写配置 |

> 这些内部接口是用「从 `.pyc` / `.pyd` 里提取字符串」的方式核实的，升级 Anki 后建议重新确认一遍。

### 5.5 统计表结构

```sql
CREATE TABLE answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ts INTEGER, cid INTEGER, nid INTEGER, did INTEGER,
  mode TEXT,            -- single / multi / tf / fill
  correct INTEGER, dontknow INTEGER, partial INTEGER,
  ms INTEGER,           -- 本题用时（毫秒）
  ease INTEGER,         -- 最终评级 1-4
  profile TEXT,         -- Anki 用户配置名
  deck TEXT, q TEXT     -- 作答当时的牌组名和题干摘要（冗余存储，避免查 Anki）
);
```

**难度公式**：`(答错次数 + 0.5) / (作答次数 + 1)`（平滑错误率，避免 1 次答错就排第一）。

### 5.6 编辑器里控件的写回（制卡这条路，2026-09-23 修过，别改回去）

选择题的「选项」、判断题的「答案」在编辑器里被换成我们自己的勾选控件，值要写进 Anki 的字段。
**关键事实：新版编辑器的字段内容不在 DOM 里。** 页面里那个 `<textarea>` 是输入代理
（`.field-container → .editor-field → .editing-area → … → .plain-text-input → .code-mirror → textarea`），
`textarea.value = "…"` 完全没用——**存出来的卡会是空字段**。这就是 1.0.1 的致命 bug：
界面看着一切正常、状态行也正常，但选项/对错根本没进笔记。

正确路线（`assets/editor.js` 发消息，`__init__.py` 负责写）：

1. JS 改动时发 `pycmd("iq:editor:set:<字段序号>:<encodeURIComponent(内容)>")`
2. Python 收到后先 `editor.saveNow(cb)` —— 这一步会把页面里**真实的**字段值同步回 `editor.note.fields`
3. 回调里把 `fields[序号]` 换成新内容，`editor.web.eval("setFields(字段名数组, 字段值数组)")`
   —— `setFields(names, values)` 是编辑器页面自己暴露的接口（Anki 载入笔记时就是这么调的），
     两个参数：字段名数组 + 字段值数组（HTML 字符串）。少传一个参数会报
     `Cannot read properties of undefined (reading 'length')`
4. 之后 Anki 自己的保存 / 点「添加」照常工作

配套细节：

- 字段初值由 Python 在注入时给（`cfg["values"] = editor.note.fields`），脚本不再去 DOM 里读——
  DOM 里读不到（见上）。注入的值会 `htmlToText()` 转成按行纯文本再解析选项。
- 没有宿主（老版本 Anki 字段就是 textarea）时，`commitField()` 退回直接写 textarea。
- 填空题的「题目」是原生输入框，脚本读不到实时内容，所以用户在「题目」里打字后
  （`input`/`blur` 事件 + 500ms 防抖）发 `iq:editor:refresh`，Python 再 `saveNow` 一次、
  把最新字段值用 `refreshCloze(values)` 推回页面刷新「共 N 个空 → N 张卡」。
- `editor.saveNow()` **不会**抢焦点（实测：写回之后焦点还在我们自己的输入框里），
  所以在用户敲键盘时同步是安全的。
- `getNoteInfo()` 在 legacy 编辑器页面里是坏的（`TypeError: Cannot read properties of undefined (reading 'id')`），
  别用它读字段。

### 5.7 「知识点」链接与「同标签技巧」

**知识点**（字段名 `知识点`，追加在三个题型的最后）：

- 字段内容是**一行一条**（1.1.6 前只看第一行），两种写法：`https://…`（网址），或 `标签 -> 链接`
  （`->` 也接受 `→`）；标签重复只留第一条。
- 目标是网址 → `aqt.utils.openLink()` 交给系统浏览器；否则当 **Anki 搜索式**，支持
  `anki:search:<搜索式>` / `anki:browse:` / `anki:tag:<标签>` / `anki:deck:<牌组>` / `anki:note:<笔记id>`，
  写成裸的 `tag:唐诗` 也行。打开方式是 `aqt.dialogs.open("Browser", mw)` + `browser.search_for(query)`。
- 解析函数 `parse_knowledge()`（Python，返回 `[(标签, 目标)]`）/ `knowledge_query()` 与
  `parseKnowledge()`（quiz.js）一套规则；注意字段里存的 HTML 会把 `>` 变成 `&gt;`，
  两边都要先 `htmlToText` 再解析。
- 卡片上：正面答完题后在反馈区末尾出一排可点的标签；答案面有自己的 `#iq-knowledge` 区块。
  桌面端点标签发 `iq:open:`；手机上（没插件）网页链接就是普通 `<a>`（能点开），搜索式则给一行
  「在 Anki 里搜：xxx」的提示。没配链接的标签不出现；字段里的老写法照旧显示。
- 编辑器里：「知识点」字段下面是 [🏷 配置标签链接]（默认收起）+ 预览行，点开逐行填
  （标签 + 输入框 + [试打开] + [×] + [🔄 刷新标签]）；试打开走的就是卡片端同一套打开逻辑。

**同标签技巧**：

- 编辑器里「解题技巧」字段下面有 [⚡ 用同标签卡片的技巧]。
- 标签从两处合起来：`note.tags`（已经保存的）+ JS 从标签框 DOM（`.tag-editor .tag`）读到的那份
  —— 用户刚敲、还没失焦提交的标签只在页面里，不带上就会误判「还没有标签」。
- 服务端（`gather_tips()`）用 `find_notes('(tag:"A" or tag:"B")')` 找同标签笔记，逐条取「解题技巧」
  字段（去掉 HTML），按共同标签数、nid 倒序排，再用 `tip_key()`（去掉所有空白）判重，最多 20 条。
- 无论几条都返回列表 `showTips({tags, items, total})`（1.1.6 起不再「一条就自动填」），
  候选行只显示技巧正文 + 共同标签、点 [用这条] 走的是同一条写回路径。

### 5.8 「答案」字段：三个题型都不要了（1.1.3 定稿）

来龙去脉（很重要，别再翻烧饼）：

1. 1.0.x 那轮「删掉答案字段」的本意只是**选择题**（正误存在选项行的 `*` 里），
   但当时把选择题、填空题的「答案」一起删了。
2. 1.1.2 把三个题型的「答案」都补了回来（追加在字段表最后，老笔记值不错位）。
3. 用户明确要求：**三个题型的「答案」字段删干净**，判断题的真值改存到题目里。
   → 1.1.3 落地：字段表去掉 `答案`，判断题的真值写进 `题目` 末尾的隐藏标记。

| 题型 | 真值存在哪儿 |
| --- | --- |
| 互动答题卡·选择题 | 选项行首的 `*`（`applyLetterAnswers()` 那条字母路径从此不再被我们的题型触发，代码留着） |
| 互动答题卡·判断题 | `题目` 末尾的 `<!--iq-tf:对-->` / `<!--iq-tf:错-->`（`TF_FLAG_RE`） |
| 互动答题卡·填空 | `{{c1::…}}` 里的原生挖空 |

判断题标记的几个要点：

- 卡片上不可见（HTML 注释）；`html_to_text` 会把整段注释去掉，所以**搜索、排序字段（sfld）、
  卷面都不会露出来**（脚本里是 `<[^>]*>` 或 Anki 自己的 HTML 解析，都不吐注释文本）。
- 迁移（`migrate_tf_flags()`）：启动时对有「答案」字段的判断题，读 对/错 → 写进题目标记 →
  往 `user_files/tf_flags.json` 记一份（note id → 对/错）→ 清空「答案」→ 删字段。
  幂等：重复跑不会写两段标记（`with_tf_marker()` 先删旧标记）。
- 自愈（`heal_tf_markers()`）：启动时 + 编辑器打开判断题时，若题目里没有标记但小账本里有记录，
  自动补回标记；两处都没有就按「还没设置」处理（卡片不接管，当成普通卡）。
- 删字段是**点名删**（`drop_field_if_empty(col, nt, "答案")`：只删字段名叫「答案」且内容为空的），
  不碰用户自己加的其它空字段、也不碰其它题型。
- 卡片端真值来源：`iq-raw-question`（模板给判断题多留了一份题目的隐藏副本）→ 渲染出来的
  `#iq-question` 里再找一遍 → 都没有就回退到老的 `iq-raw-answer`（兼容旧卡）。

### 5.9 删字段的三重保险与整理报告（1.1.4）

1.1.3 上线后实测：判断题的真值倒是全搬进标记了（14 张、`tf_flags.json` 14 条、模板也换了），
但**判断题和填空题那个空的「答案」字段没删掉**——只有选择题掉了（那是老的 `_drop_unused_fields`
路径，在 `_ensure_note_type` 里跑）。那条新路径失败了却只 print 到调试日志，沙箱里看不到，于是 1.1.4 做了三件事：

- **三条 ensure 路径都开 `drop_extra_fields=True`**：判断题、填空题也走那条"已经验证能删"的老路径。
- **`drop_field_if_empty()` 加保险**：
  - 空不空不再只信 `字段:_*` 搜索，先**直接逐条读该题型的笔记**判断（`_field_nonempty_notes()`，题型 0 条笔记时直接得 0）；
  - 删除前若字段配置带 `prevent_deletion`（或旧键 `preventDeletion`），清掉并保存题型再删；
  - 第一次失败会**重试一次**，仍失败就把异常原文写进报告（不再静默）。
- **整理报告**：启动时把结果写到 `user_files/migration.json`，形如
  `{version, ts, ensure_removed, tf_migrated, tf_healed, answer_removed_types, types: {题型: {fields_final, has_answer, reason, nonempty_notes, error}}}`。
  这样不用启动 Anki 也能核对（沙箱里直接读文件）。
- **编辑器兜底**：万一将来某种原因字段删不掉，编辑器助手会把残留的「答案」整块藏起来
  （`hideLeftoverAnswerField()`，和当年藏「选项」一个做法），保证「添加卡片」窗口里看不到它。

实测（1.1.4 在真库副本上干跑）：判断题、填空题的残留字段被删掉，选择题本来就是 `no-field`；
14 张判断题标记、小账本、其它 24 个题型全部原样。

**真机结果（2026-09-24 20:00，用户装 1.1.4 并重启后）**：`user_files/migration.json` 写着

```json
"ensure_removed": ["互动答题卡·判断题/答案", "互动答题卡·填空/答案"],
"tf_migrated": 0, "tf_healed": 0,
"answer_fields_left": []
```

也就是说：**真正生效的是 ensure 那条路（`drop_extra_fields=True`）**——在 `_ensure_note_type`
里就把那两处残留删掉了；后面的 `clean_answer_fields()` 只是复查（三个题型都成了 `no-field`）。
库核对：选择题 6 字段、判断题 5 字段、填空题 5 字段，都不含「答案」；14 张判断题题目标记
14/14 还在，模板里没有 `{{答案}}` 引用、也没有引用到不存在的字段。**1.1.3 的"点名删字段"那条路
到底为什么失败，最终没能复现**，但 1.1.4 的保险组合已经把它解决了（记这一笔，是为了以后再遇到
"字段删不掉"时先看 ensure 路径 + 报告）。

---

### 5.10 答完不再列答案文字（1.1.5）

用户原话：「选择题、判断题答完后不需要答案，用已经有的颜色就足以区分正确与否，填空题也不需要答案，
改成答完后正确答案显示在挖的空里」。

落地（`assets/quiz.js` / `assets/quiz.css` / `__init__.py` 的背面模板）：

1. **删掉反馈区那一行**：`finish()` 里原来 `if (!correct)` 渲染的
   `<div class="iq-answer-line">正确答案：…</div>` 整段去掉，`answerSummary()` 一起删。
2. **填空改成写进空里**：`lockInputs()` → `revealFillBlanks()`。判完把空位里的 `<input>` 换成静态内容：
   答对 / 答错都写卡片里的**标准答案**（该空第一个可写法的 `group[0]`，用回现成的 `.iq-blank-filled`
   绿色下划线样式）；答错且自己填过东西时，前面再放 `<s class="iq-fill-typed">你填的</s>` + `.iq-fill-arrow`
   的箭头。这个空本来就没录答案（老卡）时保持输入框着色的老样子，不做揭晓。
3. **背面同样不列答案文字**：背面模板删掉 `#iq-answer-block / #iq-answer-list`；选择题照旧画着色选项；
   判断题没有「选项」字段，`renderBack()` 现画出「正确 / 错误」两项、正确的那项 `.iq-correct`（绿），
   所以判断题的答案面也能靠颜色看对错；填空 / 原生填空的答案都在空里（原生填空本来就由 Anki 写在空里）。
4. **不新增配置项**，评分 / 统计 / 评级 / 手机模式全都不动——手机模式共用 `finish()`，所以两端一起变。

判断「是不是判断题背面」用的是现成的 `tfValue(data.answers[0])`（跟正面 `detectMode` 同一套判断）。

---

### 5.11 技巧候选瘦身 + 知识点改成「标签可点」（1.1.6）

用户原话：「优化根据标签重复利用解题技巧的功能，弹出的可重复利用解题技巧不再显示问题，且重复的
解题技巧只显示一个（根据文字查重，空格、换行符不管）。相关知识点跳转修改，显示标签，改成点击标签
跳转，每个标签对应跳转链接在添加界面决定。」

**技巧候选**（`__init__.py` 的 `gather_tips()` + `assets/editor.js` 的 `showTips()` / `buildTipRow()`）

- 服务端：候选项先按 `(-共同标签数, -nid)` 排序，**再**用 `tip_key()`（去掉所有空白：空格、换行、
  全角空格、`&nbsp;`）判重，同一段文字只留排最前面那张卡的标签信息；`total` 也按去重后算；
  payload 里**不再带 `title`**（题目）。
- 编辑器端：删掉「只有一条就直接填入」的分支，**一律弹候选面板**；每行只显示技巧正文 +
  `🏷 共同标签`；点 [用这条] 后提示只说「已填入」，不再回显题目。

**相关知识点**（`assets/quiz.js` / `quiz.css` / `editor.js`）

- 字段格式：一行一条 `标签 -> 链接`（`→` 也认）。`parse_knowledge()` 由「只取第一行、返回一对」
  改成「返回全部有效行的 `[(标签, 目标)]`」；卡片端 `parseKnowledge()` 同样按行收集、同名只留第一条。
- 卡片上：`📚 相关知识点` + 一排 `.iq-know-btn` 标签（`.iq-know-tags` 自动换行、圆角 chip），
  点谁跳谁。网址在桌面端发 `iq:open:` 交给插件、手机端就是普通 `<a>`；非网址的搜索式在手机端
  合成一行「在 Anki 里搜：…」。**没配链接的标签不出现**；字段里已有的老行（只写链接、标签不是
  卡片标签的行）照旧显示，数据一个字不动。
- 编辑器：「知识点」下面**默认收起**，只留 [🏷 配置标签链接] + 一行预览（卡片上会显示哪些标签）。
  点开是逐行面板：标签名 + 链接输入框 + [试打开] + [×]，底部 [🔄 刷新标签]。行来源 = 标签框 DOM
  （`.tag-editor .tag`，含还没提交的，沿用同标签技巧那套读法）+ 注入 payload 里的 `cfg.tags` 兜底
  （新增 `_editor_tags()`）；字段里的其它行作为「老内容」排在后面、原样保留（老格式写回时仍然
  只有链接，不会硬加标签）。失焦 / 回车即写回字段（一行一条 `标签 -> 链接`），[×] 去掉那一行。
  面板收起时不重绘行；字段值被推回来时刷新预览行 + 重画面板并保留正在输入的内容。
- 版本 / 产物：1.1.5 → 1.1.6（`version.txt`、manifest 的 `mod` 与描述同步），重出预览页
  （**7 张卡**：新增「选择题 · 答对（相关知识点 = 可点的标签）」）、zip、`.ankiaddon`；
  `一键更新插件到Anki-1.1.5.cmd` 改名成 `…-1.1.6.cmd`。
- `build-zip.py` 顺手加了一条规则：**跳过别的版本**的 `答题反馈预览-x.y.z.html` /
  `互动答题卡-源码更新-x.y.z.zip` / `一键更新插件到Anki-x.y.z.cmd`（上一版没删干净时不会被塞进新包）。
- **收尾时抓到一个自己的漏改**：`version.txt`、manifest、分发包都改成 1.1.6 了，但 `__init__.py` 里的
  `__version__` 还是 `"1.1.5"` —— 插件自更新会一直以为自己没升级。已修正，并加了
  `P253 插件里的版本号和 version.txt 一致` 钉住它（原来只有 P134「版本号是非空字符串」，拦不住）。
  **以后改版本号：`version.txt` + `__version__` + manifest 的 `mod`/描述，四处一起改。**

### 5.12 检查更新认得出「装好了但还没重启」（1.1.7）

用户报的现象：「更新后检查更新仍显示为旧版本」。查下来不是数据坏了——装到 `addons21` 的文件已经是
1.1.6（8 个文件与源码逐字节一致，写入时间 23:39:58），但那次检查发生在 **23:40:11**，比 Anki 重启
（23:40:21）早 10 秒：**跑着的还是启动时载入内存的旧代码**，于是提示「有新版本 1.1.6（当前 1.1.5）」。
重启后的实例一直没再查过，所以看着像"没修好"。

改法（`check_for_update()` + 新的 `disk_version()`）：

- `disk_version()` 直接读 `Path(__file__).parent/"__init__.py"` 里的 `__version__`（正则，不导入模块），
  拿到**磁盘上装的是哪一版**；运行中的版本仍是模块里的 `__version__`。
- 每次检查都把诊断写进 `user_files/update.json`：`checked_at` / `latest`（线上）/ `running`（运行中）/
  `on_disk`（磁盘上），另外保留 `installed`（上次真装成功的版本）。
- 判定顺序：线上不比运行版本新 → 老文案「已经是最新版本（{running}）」；线上有更新但**磁盘版本 ≥ 线上
  版本**（或 `installed` 就是这个版本）→ **不弹下载**，改提示「新版 {磁盘版本} 已经装好了，重启 Anki 后
  生效（当前运行中的还是 {running}）」；否则才走原来的下载 + `AddonManager.install`，装成功后记
  `installed = latest` 并提示重启。
- 启动时那次静默检查也守这条规则（非模态 tooltip，一天最多一次），不会再催着重复下载。

顺带把 GitHub 上缺的 Release 补齐（v1.0.0 / v1.0.1 / v1.1.0 / v1.1.1 / v1.1.6 / v1.1.7，各自附当时的
`.ankiaddon`），这样提示里的「这次改了什么」链接不再是 404，`releases/latest/download/interactive_quiz.ankiaddon`
这条兜底下载路径也真正可用。1.1.2–1.1.5 没有独立提交，不单独建 Release（v1.1.6 的说明里写明它们的内容）。

---

## 6. 关键设计决策（改动前先读）

1. **评级走 `Reviewer._answerCard`**，并带一个"点击底部按钮"的回退。不直接模拟按键。
2. **答错后默认先亮答案、再记重来**（`wrong_action = "reveal"`）。理由：立刻翻页看不到正确答案，学习效果差。
   1.1.5 起「亮答案」= 选项着色 / 空里写标准答案 / 解析（不再有单独一行 `正确答案：…`）。想要立刻翻页改成 `immediate`。
3. **强制评级的实现是 `min(用户选择, 允许上限)`**。所以答错的卡按 3 会被压成 1；漏选的卡按 3 会被压成 2，但按 1（更严格）仍然允许。
4. **漏选的判定**：选中集是正确集的真子集（漏选且零错选）才算 partial；只要选错一个就是答错。
5. **统计只记"在答题界面真正作答过"的卡**（点选项 / 提交 / 不知道）。直接按空格翻背面不算，因为那时没有判定结果。
6. **统计不塞进 `col.conf`**。Anki 源码里 `Collection.set_config` 的文档字符串原话：*"The config is currently sent on every sync, so please don't store more than a few kilobytes in it."* 每次都整包传，而且不是逐条合并。
7. **建表用 `CREATE TABLE IF NOT EXISTS` + 字段迁移**（`PRAGMA table_info` + `ALTER TABLE ADD COLUMN`），方便以后加列不丢老数据。
8. **排序必须在截断之前做**。这个坑踩过一次：早先版本先取前 500 再排序，超过 500 张时顺序是乱的。

---

## 7. 怎么验证

### 7.1 自动化测试

```powershell
$py = "C:\Program Files\Lenovo\ModelMgr\Plugins\Image\python.exe"

# Python 侧 270 项（用假的 aqt 模块导入插件）
& $py "源码\tests\py-test.py"

# 语法检查
& $py -m py_compile "源码\__init__.py"
```

卡片端 208 项 + 编辑器助手 123 项需要 JS 运行时（1.1.5 起本机有 node 24，在项目文件夹里直接跑
`run-js-tests.js` 就行；以前只有 Codex 内置的 JS 运行环境，测试还要在 %TEMP% 的源码副本里跑）：

> **教训（1.0.1 的 bug 就是这么漏掉的）**：编辑器那段以前只验到「勾选后 textarea 里变成了 `*…`」，
> 但那个 textarea 根本不是字段的真身，所以测试全绿、真人一用就存不出东西。
> **验证编辑器就必须走完整链路**：打开添加卡片窗口 → 填字段 → 点「添加」→ 直接查
> `collection.anki2` 里的 `notes.flds` / `cards`。别只看 DOM。

```js
// 编辑器助手（assets/editor.js）：
var editorFactory = function(document, window) { editor.js };
harness + tests/editor-test.js   // 用同一套最小 DOM
```

```powershell
# 卡片端 208 项 / 编辑器助手 123 项（在项目文件夹里跑，脚本自己找 源码\）
node run-js-tests.js            # 不带参数 = 卡片端
node run-js-tests.js 源码 editor # 编辑器助手

# 预览页核对 + 重新生成 + 重打 zip
node check-preview.js
python build-preview.py
python build-zip.py
```

#### 编辑器内部结构（2026-09-22 实测，Anki 26.9.2）

「添加卡片」窗口用的是 `aqt.addcards_legacy.AddCards` + `aqt.editor_legacy.Editor`，但里面的**网页是新的 Svelte 编辑器**：

- 字段容器：`div.fields` → 每个字段一个 `div.field-container` → 一个 **`<textarea>`**（不是 contenteditable！）
- 字段顺序 = DOM 顺序 = `editor.note.model()["flds"]` 顺序，所以按序号就能定位「选项」「答案」
- 工具栏钩子 `editor_did_init_buttons(buttons, editor)` 会触发，但 **buttons 必须是能 JSON 序列化的 HTML 字符串**：塞 Python 函数进去会让「添加卡片」窗口直接报 `TypeError: Object of type function is not JSON serializable`（踩过）。按钮点击走 `onclick="pycmd('iq:editor:xxx')"`，由 `webview_did_receive_js_message` 收（`context` 就是 Editor）。
- 往编辑器注入脚本用 `editor.web.eval(...)`；读字段用 `editor.web.evalWithCallback(...)`（异步回调）。
- **注入时机可能早于字段渲染**：`editor_did_load_note` 触发时 `document.querySelectorAll(".fields textarea")` 可能还是 0 个（踩过）。所以 `editor.js` 里用 `buildWhenReady()` 每 250ms 重试一次，最多 10 秒。
- **字段块的 DOM 层级很深**：从 textarea 往上要爬 **8 层**才是 `div.field-container`（`textarea → code-mirror → plain-text-input → collapsible → editing-area → editor-field → collapsible → field-container`）。一开始只爬 4 层，结果只藏住了文本框、字段名还露着。现在用 `fieldContainer()` 统一往上找（最多 14 层）。
- **编辑器会重新渲染字段**，把内联的 `display:none` 冲掉：所以隐藏时同时加 `iq-hidden` class（CSS 里 `display:none !important`），并且每 900ms 巡检一次，发现被冲掉就补回来。
- 改字段的做法：把原生 textarea 用 `style.display="none"` 藏起来，自己渲染控件，任何改动都写回 `textarea.value` 并 `dispatchEvent(new Event("input"))`——Anki 自己的保存流程照常工作。

```js
// 思路：把 quiz.js 包成一个带参数的工厂函数，再拼上 tests/dom-test.js 一起编译执行，
// 避免 new Function 被禁用的问题
const combined =
  "var quizFactory = function(document, window, pycmd, console, setTimeout, setInterval, clearInterval) {\n" +
  quizSrc + "\n};\n" + harness + "\ntry{\n" + tests + "\n}catch(e){log.push('ABORT '+e.message);}" +
  "\nlog.join('\\n') + '\\n----\\nPASS=' + pass + ' FAIL=' + fail;";
vm.runInThisContext(combined, { filename: "dom-test.js" });
```

`tests/dom-test.js` 里自带一个最小 DOM 仿真（createElement / innerHTML 解析 / querySelector / 事件派发），够跑通答题交互。

### 7.2 真机验证（改完后一定要做）

1. 重启 Anki（`Anki.exe` 在 `C:\Users\<你的用户名>\AppData\Local\Programs\Anki\`）
2. 等 35 秒左右让 profile 打开
3. 把 `%APPDATA%\Anki2\<你的 Anki 配置名>\collection.anki2`（连同 `-wal`）复制到临时目录
4. 用 sqlite3 读 `templates.config`，确认里面出现了新版本的标记

```python
import sqlite3
con = sqlite3.connect("副本路径")
cur = con.cursor()
cur.execute("select id, name from notetypes")          # 注意：不能用 name = ?，有 unicase 排序规则
ntid = [i for i, n in cur.fetchall() if n == "互动答题卡"][0]
cur.execute("select name, config from templates where ntid=?", (ntid,))
# config 是二进制块，直接 decode('utf-8','replace') 后做子串判断即可
```

判断依据：模板里出现 `iq:result:` / `iq:partial` / `iq-timer` 等新标记，就说明插件加载成功并更新了题型模板。

### 7.3 探针法：让 Anki 自己跑代码验证（很好用，别丢）

沙箱里没法用图形界面，但可以让 **Anki 自己**执行一段验证代码：

1. 建一个临时插件目录 `addons21/zz_iq_probe/__init__.py`，内容大意：

```python
import json, os, traceback
from aqt import gui_hooks, mw
from aqt.qt import QTimer

OUT = os.path.join(os.environ["TEMP"], "iq_probe.json")

def probe():
    log = {}
    try:
        import interactive_quiz as iq                      # 直接 import 正在改的插件
        log["ensure_cloze"] = iq.ensure_cloze_note_type()
        nt = mw.col.models.by_name(iq.CLOZE_NOTE_TYPE_NAME)
        log["type"] = nt["type"]                           # 1 = cloze 题型
        note = mw.col.new_note(nt)
        note["题目"] = "作者是{{c1::李白}}，{{c2::唐}}代。"
        mw.col.add_note(note, mw.col.decks.id("IQ探针牌组"))
        cards = [mw.col.get_card(c) for c in mw.col.find_cards("nid:%d" % note.id)]
        log["card_count"] = len(cards)                     # 期望 2（一个 c 号一张卡）
        log["html"] = [iq.on_card_will_show(c.question(), c, "reviewQuestion") for c in cards]
        mw.col.remove_notes([note.id])
    except Exception:
        log["error"] = traceback.format_exc()
    with open(OUT, "w", encoding="utf-8") as fh:
        json.dump(log, fh, ensure_ascii=False, indent=1)

gui_hooks.profile_did_open.append(lambda _profile=None: QTimer.singleShot(4000, probe))
```

2. 重启 Anki（`Start-Process "…\Anki.exe"`），等 40~60 秒，读 `%TEMP%\iq_probe.json`。
3. 把日志里**真实渲染出来的 HTML** 丢进 `tests/dom-test.js` 那套最小 DOM 里跑一遍卡片端脚本 → 等于端到端验证。
4. 删掉探针目录，再重启一次，让 Anki 回到干净状态。

#### 探针里测「同标签技巧 / 知识点」要注意的几点（2026-09-23 踩过）

- 一次 Anki 运行只测一个题型：关「添加卡片」窗口时字段有内容会弹「是否放弃输入」的模态框，探针会卡住。
  用环境变量（探针里是 `IQ_PROBE_JOB`）选这次跑哪个题型。
- 标签要在**页面**上设（`window.setTags([...])`），`note.tags` 里没有——用户没失焦前标签只在页面里。
  设完要等一会儿（标签 chip 渲染出来）再点技巧按钮，否则读不到标签。
- 想验证「打开链接」别真开浏览器：把 `aqt.utils.openLink` 和 `aqt.dialogs.open` 换成记录版本的桩。
  注意 `aqt.dialogs.open` 别的窗口也走它，桩里只拦 `"Browser"`，其它转给原函数，
  否则 `mw.onAddCard()` 会拿到桩、窗口根本不开（踩过）。
- 无头环境里 `execCommand('insertText')` 模拟打字时灵时不灵，探针要留一手：
  第一个字段空就说明没打进去，用 `setFields` 补一个（并在日志里注明），否则会误判成插件的问题。

两个坑：

- 关掉旧实例后**要等进程真的消失**再开新实例，否则新实例会停在「配置正在使用」，探针不会跑（这次踩过）。
- 中文界面下 Anki 自带题型在库里叫「问答 / 填空 / 图片遮盖」，**不叫 Basic / Cloze**。别按英文名去删题型，很容易误删用户自己的东西。
- **普通权限看不到 Anki 的窗口句柄**：`MainWindowHandle=0`、标题为空，不代表 Anki 挂了——那只是权限/会话隔离。要判断就加 `require_escalated` 再查一次。（这一轮曾误判成"僵尸进程"并把两个正常的 Anki 强杀过，白折腾。）

### 7.4 真机验证记录（1.1.6 收尾）

**结论先说：受限沙箱里跑不动 Anki 的「窗口」。** Anki 26.9.2 在这种环境里每次开新窗口都会在
`aqt/webview.py force_load_hack` / `setupMainWindow` 处 fail-fast（`STATUS_BREAKPOINT` 或
`0xC0000409`），根因是 QtWebEngine 要往 `%LOCALAPPDATA%\Anki\cache\QtWebEngine\...` 写缓存，
而沙箱只放行工作区和 `%TEMP%`。这次试过的补救（都记下来，别再重复踩）：

- `QTWEBENGINE_DISABLE_SANDBOX=1`、`--no-sandbox --single-process --in-process-gpu --disable-gpu
  --disk-cache-dir=<temp> --user-data-dir=<temp>`：**能把启动撑过去**（配置能打开、插件能跑），
  但一开第二个窗口（`mw.onAddCard()`）照样崩。
- 用 `explorer.exe` 转手启动：无效（进程没起来）；用 WMI `Win32_Process.Create` 起进程：被沙箱直接拒（拒绝访问）。
- 想在真实集合上验证也不行：`%APPDATA%\Anki2`、`%LOCALAPPDATA%\Anki` 的写权限申请只批了读。

**能做成的部分**（临时 base + 探针，做法见 7.3）：`%TEMP%\iq_probe_base`，`Anki.exe -b <base>`，
探针放 `<base>\addons21\zz_iq_probe\`，结果写 `%TEMP%\iq_probe_ab.json`。实测拿到：

1. 真 Anki 里插件加载到的版本就是 **1.1.6**；三个题型的样式里都出现 1.1.6 才有的
   `.iq-know-tags` / `.iq-know-label` —— 证明**真机启动时确实把新版卡片资产写进了题型模板**
   （以前这一步只有假 col 的断言，现在有真机证据）。
2. 真 Anki 渲染出来的卡片 HTML（`iq.on_card_will_show(card.question(), card, "reviewQuestion")`，
   53 KB）丢进 `tests/dom-test.js` 的伪 DOM + 真 `quiz.js` 点一遍：两颗标签
   `IQ探针标签` → `https://example.invalid/tang`、老格式裸链接 `https://example.invalid/old` →
   同一个网址；分别点击发出的正是 `iq:open:` 各自那条目标 ✓ —— 卡片端在老数据上也照旧。
3. **编辑器窗口内那半边**（候选面板不显示题目 + 去重、知识点面板逐行写回 / 刷新 / 试打开）
   在本机沙箱里没法实点：崩点就在开窗口那一步，跟插件代码无关。它由 `editor-test.js`（123 项）
  + `py-test.py` 的消息路由用例覆盖；真机实点用同一套探针，在能正常开 Anki 窗口的环境里跑一遍即可补上。

---

## 8. 已知限制

1. **自动评级只支持桌面版**。AnkiDroid / AnkiMobile 不跑 Python 插件。手机端会自动切到「手机模式」（见下），**这个已经修好了**。
2. **手机模式**：卡片端探测不到插件注入的 `window.__ANKI_QUIZ_CONFIG__` 时，选它：
   - 仍然渲染答题界面、选项可点、填空可填、本地判分照常
   - 不再发任何 `iq:*` 消息（`send()` 直接短路），也就不会出现"点了按钮变灰、卡片卡住"
   - 标题不再承诺"记为重来"，改成提示「点显示答案，再按 1/2/3/4 自己评级」
   - 代价：手机上「答错强制重来」「漏选记困难」这两条规则不生效（得自己按）
3. **统计不跨设备同步**。`user_files/stats.db` 不在集合里。见第 9 节 B。
4. 「创建 / 更新题型模板」会覆盖该题型的模板和样式，用户改过模板的话会丢。
5. 统计按用户配置（profile）隔离，切换配置看到的是另一份。
6. 原生填空是**独立题型**（「互动答题卡·填空」），旧笔记不会自动变过去，要手动新建或「更改笔记模板」转换。
7. 原生填空的答案直接读 Anki 渲染出来的 `data-cloze` 属性（26.9.2 实测有，长这样：`<span class="cloze" data-cloze="李白" data-ordinal="1">[...]</span>`）；万一以后 Anki 改掉这个，还有兜底：Python 侧解析 `{{cN::答案}}` 注入 `window.__ANKI_QUIZ_CLOZE__`。
8. 一个 c 号出现多次时，Anki 会把它渲染成多个空位，本插件会给每个空位各配一个输入框，各判各的。

---

## 9. 待定事项

### A. 手机端（已拍板：方案二，已实现）

用户在 2026-09-22 拍板选了**方案二：保留自测、评级自己按**（没选自测降级为普通卡片，也没选真机适配）。

实现方式（`assets/quiz.js`）：

- `hostPresent()`：`window.__ANKI_QUIZ_CONFIG__` 是对象 → 桌面；不是 → 手机 / AnkiWeb / 插件没加载
- `send()` 在没有宿主时直接返回 false，所有 `iq:*` 一个都不发
- 判完之后：有宿主 → 原来的三/两个评级按钮；没宿主 → `buildManualGradeHint()` 显示「点显示答案，再按 1/2/3/4」

如果以后想升级成"手机上也能自动评级"（原方案三），可以用 AnkiDroid 的 `AnkiDroidJS` / AnkiMobile 的桥，但**必须真机试**，本机没有手机环境。

### B. 统计要不要跨设备同步

同样没有拍板。给出的四条路：

1. 塞进 `col.conf` —— 会同步，但每次同步整包传、别超几 KB，且整体覆盖不逐条合并。
2. **写进题型的一个隐藏字段**（推荐）：跟着笔记同步、按笔记粒度合并；代价是每次作答都会改动笔记的修改时间，且手机端只读不写。
3. 用 Anki 自带的 revlog 推导 —— 免费同步、逐条合并、**手机上的复习也能算进来**；代价是拿不到题型 / 不知道 / 对错，只剩评级。
4. 网盘同步 `user_files` —— 不推荐，SQLite 并发写有损坏风险。

---

## 10. 环境与操作注意事项

### 10.0 仓库与自更新（2026-09-22 起）

- GitHub 仓库：<https://github.com/creeperboo/anki-interactive-quiz>（公开），本地就是桌面这个文件夹，已经是 git 仓库（`main` 分支）
- 发版流程：改代码 → 改根目录 `version.txt` → `python 安装到Anki.py`（重新打包）→ `git add -A && git commit -m "…" && git push`
- 插件里的更新检查（`__init__.py` 的 `check_for_update`）：
  - 拉 `https://raw.githubusercontent.com/creeperboo/anki-interactive-quiz/main/version.txt`
  - 比 `__version__`，新版本就问用户要不要下载
  - 下载 `.../main/interactive_quiz.ankiaddon`，然后 `mw.addonManager.install(path)`
  - **实测（26.9.2）**：`AddonManager.install(file: IO | str, manifest=None, force_enable=False) -> InstallOk | InstallError`；返回 `InstallOk(name=…, conflicts=set(), compatible=True)` 就算成功。**没有 `installFromFile` 这个方法**（一开始按老经验写的，探针一验证才发现）
  - 下载用分块读（64 KB）+ 失败重试 2 次：`raw.githubusercontent` 偶尔会卡住，第一次 60 秒超时过；改完 0.6 秒下完
  - **1.1.1 起每个文件都有两个地址**（`_download_urls()`）：先 raw，失败或下到的包不完整就换
    `https://github.com/<repo>/releases/latest/download/<文件名>`（Release 附件，另一套 CDN）。
    2026-09-23 实测 raw 连续两次都断在 41340/56255 字节（HEAD 正常、下载被重置），
    而 Release 附件一次就下完、sha256 和本地包完全一致 —— 所以加了这条兜底，
    并且**下完先验包**（`_looks_like_package()`：PK 头 + 能当 zip 打开 + 里面有 manifest.json/__init__.py），
    半截包直接丢掉换地址。
  - 启动时 8 秒后悄悄查一次，一天最多一次（记录在 `user_files/update.json`），开关是配置项 `update_check`
- 发版时除了推 `main`，还建了一个带附件的 Release（`v1.0.0`/`v1.0.1`/`v1.1.0`/`v1.1.1`），
  这样插件更新提示里的 Release 链接点得开，附件也能直接下：
  `gh release create v1.1.1 --title "…" --notes-file <md> interactive_quiz.ankiaddon`

### 路径

| 项目 | 路径 |
| --- | --- |
| Anki 程序 | `C:\Users\<你的用户名>\AppData\Local\Programs\Anki\Anki.exe`（版本 26.9.2） |
| 插件安装目录 | `C:\Users\<你的用户名>\AppData\Roaming\Anki2\addons21\interactive_quiz` |
| 统计库 | `...\interactive_quiz\user_files\stats.db` |
| 用户配置目录 | `C:\Users\<你的用户名>\AppData\Roaming\Anki2\<你的 Anki 配置名>`（配置名 <你的 Anki 配置名>） |
| 可用的 Python | `C:\Program Files\Lenovo\ModelMgr\Plugins\Image\python.exe`（3.10，本机没装正式 Python） |

### 环境限制（很重要）

- 这个环境里**只有 `%TEMP%` 可以直接写**。写桌面、`addons21`、`Downloads` 都需要向用户申请授权。
- 写桌面文本文件要用 **UTF-8 带 BOM + CRLF**，否则中文在记事本里会乱码。
- 内置浏览器和图形界面自动化都不可用，**没法自己点开对话框看效果**，UI 只能靠用户确认。
- 卡片库被 Anki 独占锁，不能直接 sqlite 打开；要先复制一份（含 `-wal`）再读。
- `notetypes` 表上有 `unicase` 自定义排序规则，`WHERE name = ?` 会报错，要么用 `LIKE`，要么取出全部在 Python 里筛。

### 常用命令

```powershell
# 重新安装 + 打包 + 校验
& "C:\Program Files\Lenovo\ModelMgr\Plugins\Image\python.exe" "安装到Anki.py"

# 重启 Anki（先正常关闭，等不到再强杀）
$p = Get-Process Anki; $p.CloseMainWindow()
Start-Process "C:\Users\<你的用户名>\AppData\Local\Programs\Anki\Anki.exe"
```
